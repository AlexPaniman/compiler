package execute;

public enum Operand {
    LT,
    BT,
    LE,
    BE,
    OR,
    AND,
    XOR,
    NOT,
    EQL,

    JZ,
    JNZ,
    JMP,

    RET,
    INVOKE,
    NATIVE,

    ADD,
    SUB,
    MUL,
    POW,
    DIV,
    MOD,

    POP,
    PUSH,

    FETCH,
    STORE,
}
