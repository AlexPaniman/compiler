fib (n) -> {
    if (n == 1 or n == 2)
        return 1;
    return fib(n - 1) + fib(n - 2);
}
sum = 0;
for (i = 1; i < 20; i = i + 1)
    sum = sum + fib(i);
while (sum != 0)
    sum = sum - (1 ** 10000);
print(sum + 2 ** 10);